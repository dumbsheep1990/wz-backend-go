# 万知后端系统微服务架构统计

## 微服务列表及结构

### 已实现的微服务

| 微服务名称 | 主要功能 | 目录结构 | 实现状态 |
|-----------|---------|---------|---------|
| admin-service | 管理员后台服务 | api/, config/, internal/, main.go | 已实现 |
| site-service | 站点管理服务 | handlers/, service/, main.go | 已实现 |
| page-service | 页面管理服务 | api/, handlers/, service/, main.go | 已实现 |
| render-service | 页面渲染服务 | handlers/, service/, main.go | 已实现 |
| user-service | 用户管理服务 | api/, config/, internal/, main.go | 已实现 |
| trade-service | 交易系统服务 | api/, config/, internal/, main.go | 已实现 |
| community-service | 社区功能服务 | handlers/, middleware/, models/, service/, static/, main.go | 已实现 |
| component-service | 组件管理服务 | api/, handlers/, service/, main.go | 已实现 |
| gateway-service | API网关服务 | config/, main.go | 已实现 |

## 微服务架构设计模式

### 统一的服务结构

大多数微服务遵循以下目录结构：

1. **api/**: 定义服务API接口，通常包含proto文件或API文档
2. **config/**: 服务配置，包括数据库连接、中间件配置等
3. **handlers/**: HTTP请求处理器，负责处理API请求
4. **service/**: 核心业务逻辑实现，包含业务处理函数
5. **internal/**: 内部使用的包，不对外暴露
6. **middleware/**: 中间件，如认证、日志、限流等
7. **main.go**: 服务入口文件，负责服务初始化和启动

### 代码组织特点

1. **按领域划分**: 每个微服务对应一个业务领域，如用户管理、站点管理等
2. **松耦合设计**: 服务之间通过API调用，减少直接依赖
3. **RESTful API**: 大部分服务采用RESTful API设计风格
4. **统一的错误处理**: 采用一致的错误码和错误响应格式
5. **中间件共享**: 通用功能如认证、日志等通过共享中间件实现

## 服务间通信模式

系统采用了以下几种服务间通信方式：

1. **HTTP/REST**: 主要用于服务间同步通信
2. **gRPC**: 用于高性能的服务间通信
3. **消息队列**: 用于异步通信和事件驱动的场景

## 微服务实现进度

目前已完成9个核心微服务的实现，还有3个微服务处于待实现状态。已实现的微服务覆盖了系统的主要功能，包括用户管理、内容管理、交易系统等核心业务领域。

## 与数据库的映射关系

每个微服务都有自己独立的数据模型，对应数据库中的不同表结构：

1. **admin-service**: 对应权限管理、系统设置相关表
2. **user-service**: 对应用户、用户详情、登录日志等表
3. **site-service**: 对应站点、主题等表
4. **page-service**: 对应页面、页面区块等表
5. **trade-service**: 对应订单、支付、产品等表
6. **community-service**: 对应社区、帖子、成员等表

