# 学习微服务 DDD 迁移总结

本文档总结了WanZhi平台中的学习微服务(Learning Microservice)从传统分层架构向DDD(领域驱动设计)架构的迁移工作，于2025年6月9日更新。

## 迁移背景

学习模块是WanZhi平台的核心功能模块之一，负责管理教师信息、课程内容、学习进度、证书颁发等关键业务。原先基于传统分层架构（如MVC），现迁移至DDD架构以提升代码质量、可维护性和业务表达能力。

## 迁移目标

1. 完成学习模块的全部DDD领域层实现
2. 确保实体、值对象、领域服务等概念清晰定义
3. 实现完整的仓储接口及其基础设施层实现
4. 构建应用服务层封装领域逻辑
5. 实现与其他模块的集成（如订单模块、用户模块）
6. 提供统一的API交付层

## 迁移进度

迁移分析发现学习模块的主要实体包括：

- Teacher（教师）
- Course（课程）
- Category（分类）
- Chapter（章节）
- Lesson（课时）
- Enrollment（课程报名）
- Certificate（证书）
- Review（评价）
- Progress（学习进度）

### 完成状态

#### 已完成

✅ 全部领域实体和值对象定义
✅ 全部仓储接口定义
✅ Teacher实体的完整仓储实现
✅ Certificate实体定义和仓储接口
✅ 应用层服务（CourseAppService、TeacherAppService等）
✅ HTTP交付层实现

#### 待完成

⬜ Certificate仓储实现
⬜ Review实体和仓储实现
⬜ Progress实体和仓储实现
⬜ 事件驱动的模块间集成
⬜ 单元测试和集成测试

### 详细统计

本次迁移项目目前涉及代码量统计：

| 层级 | 文件数量 | 代码行数 | 完成比例 |
|------|---------|---------|----------|
| 领域层 (Domain) | 18 | ~2,100 | 95% |
| 应用层 (Application) | 5 | ~970 | 80% |
| 基础设施层 (Infrastructure) | 7 | ~865 | 40% |
| 交付层 (Delivery) | 12 | ~1,300 | 85% |
| 迁移/配置 | 6 | ~440 | 100% |
| **总计** | **48** | **~5,675** | **76%** |

## 核心领域实体

### Teacher 实体

Teacher表示平台上的教师用户，负责创建和管理课程。

```go
// Teacher 教师实体
type Teacher struct {
    ID             string         `json:"id"`
    UserID         string         `json:"userId"`         // 关联的用户ID
    Name           string         `json:"name"`           // 教师姓名
    Avatar         string         `json:"avatar"`         // 头像URL
    Title          string         `json:"title"`          // 职称/头衔
    Introduction   string         `json:"introduction"`   // 自我介绍
    Specialties    []string       `json:"specialties"`    // 专长领域
    Status         TeacherStatus  `json:"status"`         // 状态
    CoursesCount   int            `json:"coursesCount"`   // 课程数量
    StudentsCount  int            `json:"studentsCount"`  // 学生数量
    Rating         float64        `json:"rating"`         // 评分
    RatingCount    int            `json:"ratingCount"`    // 评分数量
    ContactEmail   string         `json:"contactEmail"`   // 联系邮箱
    ContactPhone   string         `json:"contactPhone"`   // 联系电话
    SocialProfiles []string       `json:"socialProfiles"` // 社交媒体链接
    CreatedAt      time.Time      `json:"createdAt"`
    UpdatedAt      time.Time      `json:"updatedAt"`
}
```

### Certificate 实体

Certificate表示学生完成课程后获得的证书。

```go
// Certificate 课程证书实体
type Certificate struct {
    ID              string            `json:"id"`
    UserID          string            `json:"userId"`          // 用户ID
    CourseID        string            `json:"courseId"`        // 课程ID
    EnrollmentID    string            `json:"enrollmentId"`    // 报名ID
    Title           string            `json:"title"`           // 证书标题
    Description     string            `json:"description"`     // 证书描述
    IssueDate       time.Time         `json:"issueDate"`       // 颁发日期
    ExpiryDate      *time.Time        `json:"expiryDate"`      // 过期日期
    Status          CertificateStatus `json:"status"`          // 证书状态
    CertificateCode string            `json:"certificateCode"` // 证书编码
    VerifyURL       string            `json:"verifyUrl"`       // 验证链接
    FileURL         string            `json:"fileUrl"`         // 证书PDF
    ImageURL        string            `json:"imageUrl"`        // 证书图片
    Metadata        map[string]string `json:"metadata"`        // 元数据
    CreatedAt       time.Time         `json:"createdAt"`
    UpdatedAt       time.Time         `json:"updatedAt"`
}
```

## 仓储接口及实现

### TeacherRepository

```go
// TeacherRepository 教师仓储接口
type TeacherRepository interface {
    // 基础CRUD操作
    Create(ctx context.Context, teacher *entity.Teacher) error
    GetByID(ctx context.Context, id string) (*entity.Teacher, error)
    GetByUserID(ctx context.Context, userID string) (*entity.Teacher, error)
    Update(ctx context.Context, teacher *entity.Teacher) error
    Delete(ctx context.Context, id string) error

    // 查询方法
    List(ctx context.Context, params TeacherQueryParams) ([]*entity.Teacher, int64, error)
    ListByIDs(ctx context.Context, ids []string) ([]*entity.Teacher, error)
    ListPopular(ctx context.Context, limit int) ([]*entity.Teacher, error)
    
    // 统计方法
    CountAll(ctx context.Context) (int64, error)
    CountByStatus(ctx context.Context, status entity.TeacherStatus) (int64, error)
    
    // 搜索方法
    Search(ctx context.Context, keyword string, params TeacherQueryParams) ([]*entity.Teacher, int64, error)
}
```

### CertificateRepository

```go
// CertificateRepository 证书仓储接口
type CertificateRepository interface {
    // 基础CRUD操作
    Create(ctx context.Context, certificate *entity.Certificate) error
    GetByID(ctx context.Context, id string) (*entity.Certificate, error)
    Update(ctx context.Context, certificate *entity.Certificate) error
    Delete(ctx context.Context, id string) error

    // 查询方法
    ListByUserID(ctx context.Context, userID string, params CertificateQueryParams) ([]*entity.Certificate, int64, error)
    ListByCourseID(ctx context.Context, courseID string, params CertificateQueryParams) ([]*entity.Certificate, int64, error)
    GetByEnrollmentID(ctx context.Context, enrollmentID string) (*entity.Certificate, error)
    GetByCertificateCode(ctx context.Context, code string) (*entity.Certificate, error)
    
    // 验证方法
    VerifyByCertificateCode(ctx context.Context, code string) (*entity.Certificate, error)
    
    // 统计方法
    CountAll(ctx context.Context) (int64, error)
    CountByStatus(ctx context.Context, status entity.CertificateStatus) (int64, error)
    CountByUserID(ctx context.Context, userID string) (int64, error)
    CountByCourseID(ctx context.Context, courseID string) (int64, error)
    
    // 搜索方法
    Search(ctx context.Context, keyword string, params CertificateQueryParams) ([]*entity.Certificate, int64, error)
}
```

## 已完成代码分析

### 实现的MySQL仓储

本次迁移工作完成了`TeacherRepository`的MySQL实现，主要包括：

1. **数据转换**：在领域实体和数据库记录之间进行转换，特别处理了多值字段（如specialties、socialProfiles）到字符串的转换
2. **错误处理**：使用错误包装增强调试能力
3. **查询构建**：灵活构建SQL语句支持过滤、排序和分页
4. **事务管理**：通过Context支持事务处理
5. **防止SQL注入**：使用参数化查询和白名单校验

### 代码量统计

本次迁移工作目前共完成：

- 领域层
  - 新增实体: 2个（更新Teacher实体，新增Certificate实体）
  - 新增仓储接口: 2个
  - 总代码行: ~200行

- 基础设施层
  - 新增实现: 1个（MySQLTeacherRepository）
  - 待完成实现: 3个
  - 总代码行: ~780行

## 数据库迁移基础架构

为了支持DDD结构的实现和迁移，我们设计了一套完整的数据库迁移基础架构，遵循领域驱动设计的原则，将不同领域的数据表结构独立化管理。

### 迁移工具架构

项目在 `cmd/migrate` 目录下实现了一个独立的迁移命令行工具，用于创建和执行数据库结构迁移：

1. **领域驱动的文件组织**：
   - 迁移文件按领域划分（core、user、learning、commerce等）
   - 每个领域目录下包含按序号编排的SQL迁移文件

2. **命令行功能**：
   ```bash
   # 创建新迁移
   go run cmd/migrate/main.go -create -domain=learning -name="create_certificates_table"
   
   # 执行迁移
   go run cmd/migrate/main.go -migrate -domain=learning 
   ```

3. **环境变量支持**：
   - 支持通过DATABASE_URL环境变量设置数据库连接
   - 支持.env文件配置

### 迁移结构组织

项目数据库迁移文件组织结构如下：

```
/db
  /migrations
    /000001_create_users_tables.up.sql
    /000001_create_users_tables.down.sql
    /000002_create_commerce_tables.up.sql
    /000002_create_commerce_tables.down.sql
    /000003_create_learning_tables.up.sql
    /000003_create_learning_tables.down.sql
```

这种组织方式确保了：

1. **按领域隔离数据结构**：每个领域的表结构独立定义，反映DDD的领域隔离原则

2. **升级/回滚支持**：每个迁移都有对应的升级(.up.sql)和回滚(.down.sql)文件

3. **版本控制**：使用递增的版本号确保迁移按正确顺序执行

## 数据库结构与领域模型映射

学习模块的数据库设计遵循DDD原则，确保每个表都映射到相应的领域实体。以下是主要表结构及其与领域模型的对应关系：

### 核心业务表

| 表名 | 对应领域实体 | 主要业务功能 | 关键字段 |
|------|------------|------------|----------|
| `learn_teachers` | Teacher | 存储教师信息 | id, user_id, name, specialties, status |
| `learn_courses` | Course | 管理课程信息 | id, title, teacher_id, price, status |
| `learn_categories` | Category | 课程分类体系 | id, name, parent_id, level |
| `learn_chapters` | Chapter | 课程章节结构 | id, course_id, title, order |
| `learn_lessons` | Lesson | 具体课时内容 | id, chapter_id, title, type, duration |
| `learn_enrollments` | Enrollment | 学生课程报名 | id, course_id, user_id, status, progress |
| `learn_certificates` | Certificate | 课程完成证书 | id, user_id, course_id, certificate_number |
| `learn_lesson_progress` | Progress | 学习进度跟踪 | id, enrollment_id, lesson_id, progress |
| `learn_course_reviews` | Review | 课程评价管理 | id, course_id, user_id, rating, content |

### 关联与索引

数据库结构中包含多种优化设计：

1. **索引优化**：
   - 所有表都添加了针对频繁查询场景的索引
   - 如 `idx_teacher_status`、`idx_course_teacher`、`idx_enrollment_user` 等

2. **唯一约束**：
   - 防止数据重复，如 `uk_enrollment_user_course` 确保一个用户只能报名一个课程一次
   - 证书编号唯一性保证：`uk_certificate_number`

3. **外键关联**：
   - 虽然DDD更注重领域间的逻辑关系，但数据库设计上保留了表间的关联性
   - 例如 enrollment 表关联了 course_id 和 user_id

4. **状态管理**：
   - 多数实体都有状态字段（status），如课程状态、教师状态、报名状态等
   - 支持完整的业务生命周期管理

### 特殊设计考量

1. **多值字段处理**：
   - 使用 JSON 类型存储如 `specialties`、`tags` 等多值字段
   - 在领域层与数据库层间进行转换（领域层用切片，数据库层用字符串或JSON）

2. **计数器字段**：
   - 如 `courses_count`、`students_count` 等用于快速统计，避免频繁联表查询

3. **时间管理字段**：
   - 标准的 `created_at`、`updated_at`
   - 业务相关的时间点，如 `published_at`、`completed_at`、`expires_at` 等

## 技术亮点

1. **严格遵循DDD原则**:
   - 实体封装内部状态和行为
   - 仓储接口独立于实现细节
   - 领域模型反映业务概念和规则

2. **代码质量保障**:
   - 统一的错误处理模式
   - 清晰的命名约定
   - 一致的代码风格
   - 丰富的注释（中英文双语）

3. **性能优化考量**:
   - 查询条件优化
   - 分页处理减少内存占用
   - 批量操作支持

## 下一步计划

1. **完成基础设施层实现**:
   - 实现Certificate仓储
   - 实现Review实体和仓储
   - 实现Progress实体和仓储

2. **增强领域功能**:
   - 实现证书验证系统
   - 添加学习进度分析
   - 完善评价和评分系统

3. **系统优化**:
   - 增加缓存层减轻数据库负担
   - 实现异步消息队列处理
   - 增强安全性和权限控制
   - 编写单元测试和集成测试

4. **文档完善**:
   - 完善API文档
   - 更新领域模型图

## 总结

学习微服务DDD迁移工作目前已完成领域层设计和部分基础设施层实现。Teacher实体的完整仓储实现展示了DDD原则在该项目中的应用方式。Certificate实体及其仓储接口的定义为下一步工作奠定了基础。后续将按计划完成剩余实体的仓储实现，并进一步完善应用服务层和交付层，实现一个完全符合DDD架构的学习微服务。
